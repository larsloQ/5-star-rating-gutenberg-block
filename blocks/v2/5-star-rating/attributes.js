/**
 * to have this working in editor
 * make sure that attributes here (in JS) and PHP are the same
 */
const attributes = {
	'rating': {
		type: 'number',
		default: 3
	},
	scale: { 
		type: 'number',
		default:1
	},
	className: {
		type: 'string',
	},
};
export default attributes;
